apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-java-app.fullname" . }}-test-connection"
  labels:
    {{- include "hello-java-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-java-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
